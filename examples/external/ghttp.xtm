;;; ghttp.xtm:examples -- libghttp examples in extempore

;; Author: Ben Swift
;; Keywords: extempore
;; Required dylibs: libghttp

;;; Commentary:

;; 

;;; Code:

(sys:load "libs/external/ghttp.xtm")

;; GET

(ghttp_get_dump_response "http://example.com")

;; POST

(ghttp_post_dump_response "http://httpbin.org/post" "my-extempore-token")
